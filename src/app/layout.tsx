import type { Metadata, Viewport } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Providers from '@/components/Providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ApprovalFlow - AI-Powered Approval Workflow Platform',
  description: 'Streamline approval workflows across different departments and industries with AI-powered automation.',
  keywords: 'approval workflow, business process automation, AI workflows, enterprise software',
  authors: [{ name: 'ApprovalFlow Team' }],
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}
